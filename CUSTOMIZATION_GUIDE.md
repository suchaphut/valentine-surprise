# 🎨 คู่มือการปรับแต่งเว็บไซต์วันวาเลนไทน์

เว็บไซต์ของคุณสามารถปรับแต่งได้ตามต้องการ ไฟล์ทั้งหมดอยู่ในไฟล์เดียว `index.html`

## 🔧 การปรับแต่งพื้นฐาน

### 1️⃣ เปลี่ยน PIN (รหัสผ่าน)

เปิดไฟล์ `index.html` และค้นหาส่วน `CONFIG` (ประมาณบรรทัดที่ 300):

```javascript
const CONFIG = {
    correctPin: "1402",  // 👈 เปลี่ยนตรงนี้
    loverName: "ที่รักของฉัน",
    galleryImages: [...]
};
```

**ตัวอย่าง:**
- `"1402"` = 14 กุมภาพันธ์ (Valentine's Day)
- `"2508"` = 25 สิงหาคม (วันครบรอบตัวอย่าง)
- `"0101"` = 1 มกราคม (วันปีใหม่)

### 2️⃣ เปลี่ยนชื่อแฟน

```javascript
const CONFIG = {
    correctPin: "1402",
    loverName: "น้องสาว",  // 👈 เปลี่ยนตรงนี้
    galleryImages: [...]
};
```

### 3️⃣ เปลี่ยนข้อความหวาน

ค้นหา `love-message` ในไฟล์ HTML (ประมาณบรรทัดที่ 200):

```html
<p class="love-message" id="loveMessage">
    ในวันพิเศษนี้ ฉันอยากบอกว่า<br>
    คุณคือสิ่งที่สวยงามที่สุดในชีวิตของฉัน<br>
    ขอบคุณที่อยู่เคียงข้างฉันเสมอ<br>
    และทำให้ทุกวันเป็นวันที่พิเศษ<br>
    ฉันรักคุณมากๆ 💕
</p>
```

**เคล็ดลับ:** ใช้ `<br>` เพื่อขึ้นบรรทัดใหม่

### 4️⃣ เปลี่ยนรูปภาพ

#### วิธีที่ 1: ใช้ URL จากอินเทอร์เน็ต (ง่ายที่สุด)

```javascript
const CONFIG = {
    correctPin: "1402",
    loverName: "ที่รักของฉัน",
    galleryImages: [
        "https://images.unsplash.com/photo-1518895949257-7621c3c786d7?w=500&h=500&fit=crop",
        "https://images.unsplash.com/photo-1516062423079-7ca13cdc7f5a?w=500&h=500&fit=crop",
        // เพิ่มลิงก์รูปอีก 4 รูป
    ]
};
```

**ที่มาของรูป:**
- [Unsplash](https://unsplash.com) - รูปสวยๆ ฟรี
- [Pexels](https://pexels.com) - รูปฟรีอีกแหล่ง
- [Pixabay](https://pixabay.com) - รูปฟรีมากมาย

#### วิธีที่ 2: ใช้รูปในโฟลเดอร์ท้องถิ่น

1. สร้างโฟลเดอร์ `images` ในที่เดียวกับ `index.html`
2. ใส่รูปภาพ 6 รูปลงในโฟลเดอร์ `images`
3. เปลี่ยนลิงก์เป็น:

```javascript
galleryImages: [
    "images/photo1.jpg",
    "images/photo2.jpg",
    "images/photo3.jpg",
    "images/photo4.jpg",
    "images/photo5.jpg",
    "images/photo6.jpg",
]
```

## 🎨 การปรับแต่งสี

### เปลี่ยนสี Gradient พื้นหลัง

ค้นหา `background: linear-gradient` ในส่วน `<style>`:

```css
body {
    background: linear-gradient(135deg, #ffd6e8 0%, #ffb3d9 50%, #ff99cc 100%);
    /* 👆 เปลี่ยนสีตรงนี้ */
}
```

**ตัวอย่างสีอื่นๆ:**

**สีม่วง-ชมพู:**
```css
background: linear-gradient(135deg, #e0c3fc 0%, #f093fb 100%);
```

**สีแดง-ส้ม:**
```css
background: linear-gradient(135deg, #ff6b6b 0%, #ff8e53 100%);
```

**สีน้ำเงิน-ม่วง:**
```css
background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
```

**สีเขียว-ฟ้า:**
```css
background: linear-gradient(135deg, #84fab0 0%, #8fd3f4 100%);
```

### เปลี่ยนสีปุ่ม

ค้นหา `.pin-button` หรือ `.kiss-button`:

```css
.pin-button {
    background: linear-gradient(135deg, #ff1493 0%, #d91e63 100%);
    /* 👆 เปลี่ยนสีตรงนี้ */
}
```

## 🔤 การปรับแต่งฟอนต์

### เปลี่ยนฟอนต์

ค้นหา `@import` ในส่วน `<head>`:

```html
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700;900&family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
```

**ฟอนต์อื่นๆ จาก Google Fonts:**

**Serif สวยงาม:**
```html
<link href="https://fonts.googleapis.com/css2?family=Cormorant+Garamond:wght@700&family=Lora:wght@400;500&display=swap" rel="stylesheet">
```

**Modern Minimalist:**
```html
<link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=Open+Sans:wght@400;500&display=swap" rel="stylesheet">
```

**Playful Fun:**
```html
<link href="https://fonts.googleapis.com/css2?family=Fredoka:wght@700&family=Quicksand:wght@400;500&display=swap" rel="stylesheet">
```

## ⚡ การปรับแต่งแอนิเมชัน

### เปลี่ยนความเร็ว Animation

ค้นหา `animation:` ในไฟล์ CSS:

```css
.love-main-text {
    animation: slideDown 1s ease-out;  /* 👈 เปลี่ยน 1s เป็น 2s ให้ช้าลง */
}
```

**ตัวอย่าง:**
- `0.5s` = เร็วมาก
- `1s` = ปกติ (ค่าเริ่มต้น)
- `2s` = ช้า
- `3s` = ช้ามาก

### เปลี่ยนประเภท Animation

```css
animation: slideDown 1s ease-out;
/*          👆 ประเภท animation */
```

**ประเภท Animation ที่มี:**
- `fadeIn` - ค่อยๆ ปรากฏ
- `slideDown` - เลื่อนลงมา
- `slideUp` - เลื่อนขึ้นไป
- `float` - ลอยขึ้น
- `shake` - สั่นสะเทือน

## 📱 การปรับแต่ง Responsive

### เปลี่ยนขนาดตัวอักษร

ค้นหา `font-size` ในไฟล์ CSS:

```css
.love-main-text {
    font-size: 4rem;  /* 👈 เปลี่ยนตรงนี้ */
}
```

**ตัวอย่าง:**
- `2rem` = เล็ก
- `3rem` = ปกติ
- `4rem` = ใหญ่ (ค่าเริ่มต้น)
- `5rem` = ใหญ่มาก

## 🔐 การปรับแต่งขั้นสูง

### เปลี่ยนจำนวนรูปภาพ

ถ้าต้องการ 9 รูปแทน 6 รูป:

1. เพิ่มรูปใน `galleryImages`:
```javascript
galleryImages: [
    "images/photo1.jpg",
    "images/photo2.jpg",
    // ... เพิ่มเป็น 9 รูป
]
```

2. เปลี่ยน CSS Grid:
```css
.gallery-grid {
    grid-template-columns: repeat(3, 1fr);  /* 3 รูปต่อแถว */
    /* เปลี่ยนเป็น repeat(3, 1fr) สำหรับ 3x3 */
}
```

### เปลี่ยนขนาดรูปภาพ

```css
.gallery-item {
    aspect-ratio: 1;  /* 👈 เปลี่ยนตรงนี้ */
}
```

**ตัวอย่าง:**
- `1` = สี่เหลี่ยมจัตุรัส (ค่าเริ่มต้น)
- `16/9` = ภาพยนตร์ (กว้าง)
- `4/3` = ภาพถ่าย (กว้างปกติ)
- `9/16` = มือถือ (สูง)

## 💾 การบันทึกและ Push

หลังจากปรับแต่งเสร็จ:

```bash
# 1. บันทึกไฟล์
# (ใช้ Text Editor ที่คุณชอบ)

# 2. Commit ไปยัง Git
git add index.html
git commit -m "Customize Valentine's website"

# 3. Push ไปยัง GitHub
git push origin master
```

GitHub Pages จะอัปเดตอัตโนมัติ!

## 🆘 ปัญหาและวิธีแก้

### ปัญหา: เปลี่ยนแล้วแต่ไม่เห็นการเปลี่ยนแปลง
**วิธีแก้:**
1. ลบ Browser Cache (Ctrl+Shift+Delete)
2. เปิด Incognito Mode
3. รอ 5 นาทีให้ GitHub Pages อัปเดต

### ปัญหา: เว็บไซต์ขาด (Broken)
**วิธีแก้:**
1. ตรวจสอบ Syntax ของ JavaScript
2. ตรวจสอบ URL ของรูปภาพ
3. เปิด Browser Console (F12) เพื่อดู Error

### ปัญหา: รูปภาพไม่แสดง
**วิธีแก้:**
1. ตรวจสอบว่า URL ของรูปถูกต้อง
2. ลองใช้ URL อื่นจาก Unsplash
3. ตรวจสอบว่ารูปในโฟลเดอร์ `images` มีชื่อถูกต้อง

## 📚 ทรัพยากรเพิ่มเติม

- [Google Fonts](https://fonts.google.com) - ฟอนต์ฟรี
- [Unsplash](https://unsplash.com) - รูปฟรี
- [Color Hunt](https://colorhunt.co) - แนวคิดสี
- [CSS Gradients](https://cssgradient.io) - สร้าง Gradient

---

**Happy Customizing! 💖**

หากมีคำถาม สามารถติดต่อได้ที่ GitHub Issues
